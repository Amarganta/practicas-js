import React from "react";
import { Layout, Main } from "../../components";

export const Ejemplo = ({ titulo }) => {
  return (
    <Layout>
      <Main>
        <h1>Ejemplo</h1>
      </Main>
    </Layout>
  );
};
