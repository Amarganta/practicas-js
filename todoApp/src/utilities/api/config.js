import { axios } from "axios";
const api = axios.create({
  baseURL: "https://todo-70f99-default-rtdb.firebaseio.com/",
});
// exportar
// crear otro archivo llamado tarea dentro de la carpeta api
// en ese archivo crear los endpoints
